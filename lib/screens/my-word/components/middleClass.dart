import 'package:flutter/material.dart';

class MiddleClass extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("중급 단어"),
    );
  }
}
