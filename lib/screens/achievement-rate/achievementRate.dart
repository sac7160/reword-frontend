import 'package:flutter/material.dart';

class AchievementRateScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("AchievementRate"),
      ),
    );
  }
}
